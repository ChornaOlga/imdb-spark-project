"""
This module defines string literals for IMDb dataset column names.

Variables:
    nconst: String literal for the 'nconst' column in the name.basics dataset.
    primaryName: String literal for the 'primaryName' column in the name.basics dataset.
    birthYear: String literal for the 'birthYear' column in the name.basics dataset.
    deathYear: String literal for the 'deathYear' column in the name.basics dataset.
    primaryProfession: String literal for the 'primaryProfession' column in the name.basics dataset.
    knownForTitles: String literal for the 'knownForTitles' column in the name.basics dataset.
    titleId: String literal for the 'titleId' column in the title.akas dataset.
    ordering: String literal for the 'ordering' column in the title.akas dataset.
    title: String literal for the 'title' column in the title.akas dataset.
    region: String literal for the 'region' column in the title.akas dataset.
    language: String literal for the 'language' column in the title.akas dataset.
    types: String literal for the 'types' column in the title.akas dataset.
    attributes: String literal for the 'attributes' column in the title.akas dataset.
    isOriginalTitle: String literal for the 'isOriginalTitle' column in the title.akas dataset.
    tconst: String literal for the 'tconst' column in the title.basics dataset.
    titleType: String literal for the 'titleType' column in the title.basics dataset.
    primaryTitle: String literal for the 'primaryTitle' column in the title.basics dataset.
    originalTitle: String literal for the 'originalTitle' column in the title.basics dataset.
    isAdult: String literal for the 'isAdult' column in the title.basics dataset.
    startYear: String literal for the 'startYear' column in the title.basics dataset.
    endYear: String literal for the 'endYear' column in the title.basics dataset.
    runtimeMinutes: String literal for the 'runtimeMinutes' column in the title.basics dataset.
    genres: String literal for the 'genres' column in the title.basics dataset.
    directors: String literal for the 'directors' column in the title.crew dataset.
    writers: String literal for the 'writers' column in the title.crew dataset.
    parentTconst: String literal for the 'parentTconst' column in the title.episode dataset.
    seasonNumber: String literal for the 'seasonNumber' column in the title.episode dataset.
    episodeNumber: String literal for the 'episodeNumber' column in the title.episode dataset.
    category: String literal for the 'category' column in the title.principals dataset.
    job: String literal for the 'job' column in the title.principals dataset.
    characters: String literal for the 'characters' column in the title.principals dataset.
    averageRating: String literal for the 'averageRating' column in the title.ratings dataset.
    numVotes: String literal for the 'numVotes' column in the title.ratings dataset.
"""
nconst = 'nconst'
primaryName = 'primaryName'
birthYear = 'birthYear'
deathYear = 'deathYear'
primaryProfession = 'primaryProfession'
knownForTitles = 'knownForTitles'
titleId = 'titleId'
ordering = 'ordering'
title = 'title'
region = 'region'
language = 'language'
types = 'types'
attributes = 'attributes'
isOriginalTitle = 'isOriginalTitle'
tconst = 'tconst'
titleType = 'titleType'
primaryTitle = 'primaryTitle'
originalTitle = 'originalTitle'
isAdult = 'isAdult'
startYear = 'startYear'
endYear = 'endYear'
runtimeMinutes = 'runtimeMinutes'
genres = 'genres'
directors = 'directors'
writers = 'writers'
parentTconst = 'parentTconst'
seasonNumber = 'seasonNumber'
episodeNumber = 'episodeNumber'
category = 'category'
job = 'job'
characters = 'characters'
averageRating = 'averageRating'
numVotes = 'numVotes'
